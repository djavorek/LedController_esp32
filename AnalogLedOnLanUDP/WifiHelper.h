#ifndef WIFI_HELPER_H
#define WIFI_HELPER_H

// Connects to WiFi, sets hotspot if needed (to set credentials on)
void ConnectToWifi(char ssid[], char password[], char* deviceName);

#endif /* WIFI_HELPER_H */
